import ReactDOM from 'react-dom/client'; // by doing this we can use react features in our project.

import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
